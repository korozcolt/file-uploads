# Environment variables example
# Default PORT (can be 5500, 5000, 8888, etc.). If not specified, the app uses 5500.
PORT=5500
NODE_ENV=production
# Secret used to sign access tokens for image URLs — change to a secure random value in production
JWT_SECRET=changeme_should_be_changed
# URL expiration in hours (default 24)
URL_EXPIRATION_HOURS=24
# Max upload size in bytes (default 5MB)
MAX_UPLOAD_SIZE_BYTES=5242880
# Optional: public host used when generating signed URLs (e.g. https://example.com)
PUBLIC_HOST=
# Optional: path for DB file (default: data.sqlite in current directory)
# For Docker/Dokploy deployments, use a mounted volume path like /data/db.sqlite
DB_PATH=
# Optional: path for uploaded images storage (default: ./storage)
# For Docker/Dokploy deployments, use a mounted volume path like /data/storage
STORAGE_PATH=
# Admin credentials for the single admin user (used by /auth/login)
ADMIN_USER=admin
ADMIN_PASSWORD=changeme
